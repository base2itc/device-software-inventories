Client Setup Paket
==================

Dieses Paket enthält drei PowerShell-Skripte zur automatisierten Client-Einrichtung:

1. Enable-BitLockerSmart.ps1
   - Aktiviert BitLocker und speichert den Recovery Key.

2. Initialize-ClientSetup.ps1
   - Führt grundlegende Client-Initialisierungen durch (Updates, Name, Admin-Konto).

3. Install-StandardAppsWithWinget.ps1
   - Installiert Standardprogramme mittels Winget, inkl. automatischer Reparatur und Neustartoption.

Ausführung:
-----------
Alle Skripte sollten in einer **administrativen PowerShell** ausgeführt werden.
Weitere Hinweise zur Nutzung befinden sich direkt in den Skripten (Kommentare am Anfang).

